# ============================================================
# .env.example
# 実際に使用する場合は .env にコピーして値を設定すること
# このファイルは Git にコミットしてよい（値は入れない）
# .env 本体は .gitignore に追加して Git 管理外にすること
# ============================================================

# API Gateway のエンドポイントURL（sam deploy 後の ApiEndpoint Output の値）
VITE_API_BASE_URL=https://YOUR_API_ID.execute-api.ap-northeast-1.amazonaws.com/Prod

# Google Maps API キー
VITE_GOOGLE_MAPS_KEY=YOUR_GOOGLE_MAPS_API_KEY

# ============================================================
# 認証（Cognito + Google）
# いずれも sam deploy 後の Outputs（UserPoolId / UserPoolClientId /
# CognitoHostedUiDomain）から値を設定する。3つとも揃わないとログイン機能が動かない
# （閲覧系ページはこれらが未設定でも表示は可能）
# ============================================================
VITE_AWS_REGION=ap-northeast-1
VITE_COGNITO_USER_POOL_ID=YOUR_USER_POOL_ID
VITE_COGNITO_CLIENT_ID=YOUR_USER_POOL_CLIENT_ID
VITE_COGNITO_DOMAIN=https://studio-search-app-YOUR_ACCOUNT_ID.auth.ap-northeast-1.amazoncognito.com
